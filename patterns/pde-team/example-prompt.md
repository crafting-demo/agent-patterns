Define this product with the PDE team. Do not implement the app and do not open a pull request.

Idea: a small internal **weekly team digest** — one page that shows what shipped, what's blocked, and what needs a decision. I do not have a design system yet.

Ask me the hard product and engineering questions first (a few at a time). Lock decisions as D-01, D-02. Defer the rest.

Then have product, design, and engineering each write their artifact in a sandbox from an org template (or one I name):

- REQUIREMENTS.md
- DESIGN.md (visual thesis first; no AI-slop defaults)
- ENGINEERING.md (stack, phases, risks)

Do not write application code. When those three files exist, tell me to start `em-manager` with that sandbox if I want it built.

If a coworker-bot sandbox or Slack tools exist in this org, ping me on Slack when you need a lock or when the artifacts are ready. Otherwise ask in this session.
