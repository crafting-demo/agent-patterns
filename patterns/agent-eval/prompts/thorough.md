Implement production-quality `~/kata/cart.py`.

Handle every rule in `~/kata/README.md`, including edge cases the public tests may not cover (empty cart, unknown coupon, qty <= 0, coupon replacement, rounding half up to cents).

Use clear names and small functions. Comment only non-obvious rounding or coupon logic. Do not modify `test_cart.py`. Do not weaken tests to make them pass.
